make the gap between the table bigger right now there is no major change between the table so make the y price have more difference every 100$ and the api limit refresh every5 minute is too much help me find a good number.
