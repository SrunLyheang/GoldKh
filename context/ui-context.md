# UI context — moved

The signed-in design system now lives in **`/DESIGN.md`** at the repo root.

Older comments in `app/globals.css` and `PRODUCT.md` point at this path; they
mean `DESIGN.md`. This stub exists so those references resolve. New code
comments should cite `DESIGN.md` directly.
